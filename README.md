# MyExp
MyExp — a local-first personal expense tracker (React Native + Expo + SQLite). No backend, no account, no AI. Clean Screen → Service → SQLite architecture with Siri voice intake via deep links.
